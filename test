Sub UpdateSeriesNames()
    Dim ch As ChartObject
    Dim s As Series
    
    For Each ch In ActiveSheet.ChartObjects
        For Each s In ch.Chart.SeriesCollection
            s.Name = Replace(s.Name, "2025", "2026")
        Next s
    Next ch
End Sub
